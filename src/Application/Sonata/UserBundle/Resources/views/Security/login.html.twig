{% extends "ApplicationSonataUserBundle::layout.html.twig" %}

{% block fos_user_content %}
{% if error %}
    <div>{{ error|trans({}, 'FOSUserBundle') }}</div>
{% endif %}
{% if not is_granted("ROLE_USER") %}
    <div class="col-md-6" style="margin-left: 25%">
<div class="panel panel-success">
    <div class="panel-heading">
        <h2 class="panel-title"><i class="glyphicon glyphicon-user" style="color: #008000"></i> Autenticación</h2>
    </div>
    <div class="panel-body">
        <form class="form-horizontal" role="form" action="{{ path("fos_user_security_check") }}" method="post">
            <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
            <div class="form-group">
                <label for="username" class="col-sm-1 control-label" style="color: #008000"></label>
                <div class="col-sm-10">
                    <input type="text" class="form-control" id="username" name="_username" placeholder="{{ 'security.login.username'|trans({}, 'FOSUserBundle') }}" required="required">
                </div>
            </div>
            <div class="form-group">
                <label for="password" class="col-sm-1 control-label" style="color: #008000"></label>
                <div class="col-sm-10">
                    <input type="password" class="form-control" id="password" name="_password" placeholder="{{ 'security.login.password'|trans({}, 'FOSUserBundle') }}" required="required">
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <div class="checkbox">
                        <label for="remember_me">
                            <input type="checkbox" id="remember_me" name="_remember_me" value="on"/>
                            {{ 'security.login.remember_me'|trans({}, 'FOSUserBundle') }}
                        </label>
                    </div>
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-offset-8">
                    <a href="{{ path('fos_user_registration_register') }}" class="btn btn-primary">Registrarse</a>
                    <button type="submit" id="_submit" name="_submit" class="btn btn-success">{{ 'security.login.submit'|trans({}, 'FOSUserBundle') }} </button>
                </div>
            </div>
        </form>
    </div>
</div>
    </div>
{% endif %}


{% endblock fos_user_content %}
